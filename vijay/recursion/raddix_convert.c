#include<stdio.h>
int hexd(int,int);
int res,rem;
void main()
{
	int num=102,rad=16;
	printf("%d",res= hexd(num,rad));
}

int hexd(int num,int rad)
{
	if(num == 0) return res;
	else
	{
		rem=num%rad;
		num=num/10
		res=
	}
}
